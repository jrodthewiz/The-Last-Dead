import {createRequire} from 'node:module';import{writeFile}from'node:fs/promises';const require=createRequire(import.meta.url);const {chromium}=require(process.env.PLAYWRIGHT_PATH || 'C:/Users/wolfk/Desktop/dogfight/node_modules/playwright');const b=await chromium.launch({headless:true});try{const p=await b.newPage({viewport:{width:900,height:900}});const errors=[];p.on('pageerror',e=>errors.push(e.message));for(const [name,query]of[['front',''],['side','?angle=1.57'],['down','?down=1'],['face','?face=1']]){if(process.argv[3]&&name!==process.argv[3])continue;await p.goto('http://127.0.0.1:5200/survivor-review.html'+query,{waitUntil:'domcontentloaded'});await p.waitForFunction(()=>window.review);await p.waitForTimeout(500);await p.screenshot({path:'docs/survivor/'+(process.argv[2]||'blockout')+'-'+name+'.png'});}console.log(JSON.stringify({errors}));}finally{await b.close();}
